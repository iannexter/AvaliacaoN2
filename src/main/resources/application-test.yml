spring:
  datasource:
    url: jdbc:mysql://localhost:3306/taskdb
    username: root
    password: tifull7.aprendizado
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest


  application:
    name: task-manager-1
